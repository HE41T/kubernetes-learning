# Kubernetes Deployment Guide (อัปเดตข้อมูลล่าสุด)

หัวข้อนี้พูดถึง **Deployment** ซึ่งเป็นพระเอกหลักของการใช้งาน Kubernetes ในปัจจุบัน

---

## 1️⃣ Deployment คืออะไร? ทำไมต้องใช้?

- **Deployment** = เวอร์ชันอัปเกรดของ ReplicationController/ReplicaSet  
- หน้าที่หลัก: ควบคุมจำนวน Pod + จัดการ **Version Control**  
- ความสามารถเด่น:
  - **Update**: อัปเกรดแอปจาก v1 → v2 ได้อย่างเนียนๆ
  - **Rollback**: ถ้าอัปเดตแล้วมีปัญหา สามารถย้อนกลับเวอร์ชันเก่า
  - **Pause/Resume**: หยุดการอัปเดตกลางคันเพื่อตรวจสอบ

**เปรียบเทียบลำดับชั้น:**  
```
Pod        = พนักงาน (ทำงาน)
ReplicaSet = หัวหน้างาน (ควบคุมจำนวน Pod)
Deployment = ผู้จัดการ (ควบคุม ReplicaSet + วางแผนเวอร์ชัน)
```

---

## 2️⃣ Deployment Strategies (ยุทธวิธีการอัปเดต)

### A. Recreate (ทุบแล้วสร้างใหม่)
- ฆ่า Pod เก่าทั้งหมด → สร้าง Pod ใหม่
- **ข้อดี:** รวดเร็ว, ไม่เปลืองทรัพยากร
- **ข้อเสีย:** Downtime (เว็บล่มช่วงเปลี่ยน)

### B. Rolling Update (ทยอยเปลี่ยน – ค่า Default)
- สร้าง Pod ใหม่ทีละน้อย → ลบ Pod เก่าทีละน้อย
- **ข้อดี:** Zero Downtime, ผู้ใช้งานใช้งานต่อเนื่อง
- **ข้อเสีย:** ใช้เวลานานขึ้นเล็กน้อย, รันทั้งเก่า+ใหม่ชั่วคราว

---

## 3️⃣ คำสั่ง Deployment ที่ควรรู้

### สร้าง Deployment พร้อมบันทึกประวัติ
```bash
kubectl create -f Deployment.yaml --record
```
- `--record`: บอก K8s จดบันทึกคำสั่งไว้ใน History เพื่อให้ Rollback ได้

### อัปเดตเวอร์ชัน Image
```bash
kubectl set image deployment/<deployment-name> <container-name>=<new-image>:<tag>
```
**ตัวอย่าง:**
```bash
kubectl set image deployment/Deployment tomcat=tomcat:6.0
```
- จะเริ่ม Rolling Update ทันที

### ตรวจสอบสถานะการอัปเดต
```bash
kubectl rollout status deployment/<deployment-name>
```
- แสดงความคืบหน้าของ Pod ที่เปลี่ยน

### ย้อนกลับ (Rollback)
```bash
kubectl rollout undo deployment/<deployment-name> --to-revision=<revision-number>
```
- ตัวอย่าง:  
```bash
kubectl rollout undo deployment/Deployment --to-revision=2
```
- ถ้า Image ตัวใหม่มีบั๊ก สามารถย้อนกลับได้ทันที

---

## 4️⃣ ข้อสังเกต API Version
- บทความเก่าใช้: `extensions/v1beta1` (deprecated)
- ปัจจุบัน Kubernetes 1.16+ ใช้: `apps/v1`  
- โครงสร้าง YAML เหมือนเดิม เปลี่ยนแค่ `apiVersion`

---

## 📌 สรุป
- **Deployment** = เครื่องมือหลักดูแล Application  
- ช่วยให้:
  - อัปเกรดแอปโดยเว็บไม่ล่ม
  - ย้อนกลับเมื่อพัง
  - ควบคุม ReplicaSet และจำนวน Pod อัตโนมัติ
- ใช้ Deployment แทนการสร้าง ReplicaSet/RC โดยตรง เป็น Best Practice

