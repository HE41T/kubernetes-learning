# Kubernetes Pod Manager (ReplicationController & ReplicaSet)

หัวข้อนี้พูดถึง **ผู้จัดการ Pod (Controller)** ซึ่งเป็นคอนเซปต์สำคัญใน Kubernetes เพราะ Pod ที่สร้างขึ้นโดดๆ (Naked Pod) ถ้าตายแล้วจะไม่มีใครฟื้นฟูให้

Kubernetes จึงมี Controller มาคอยดูแลจำนวน Pod ให้ตรงตามที่กำหนด

---

## 1️⃣ ReplicationController (RC) - รุ่นเก่า

**หน้าที่หลัก:**  
- ตรวจสอบจำนวน Pod ตลอดเวลา  
- ถ้า Pod น้อยกว่าที่กำหนด → สร้างใหม่  
- ถ้า Pod มากกว่าที่กำหนด → ลบ Pod ทิ้ง

**ตัวอย่าง YAML (RC):**  
```yaml
apiVersion: v1
kind: ReplicationController
metadata:
  name: tomcat-rc
spec:
  replicas: 3
  selector:
    app: tomcat
  template:
    metadata:
      labels:
        app: tomcat
    spec:
      containers:
      - name: tomcat
        image: tomcat:8.0
        ports:
        - containerPort: 7474
```

**ข้อจำกัด:**  
- Selector รองรับเฉพาะ Equality-based (เท่ากับ) เช่น `app = tomcat`  

---

## 2️⃣ ReplicaSet (RS) - รุ่นใหม่

**หน้าที่หลัก:**  
- คล้ายกับ RC คือควบคุมจำนวน Pod ให้ตรงตาม `replicas`  
- แต่ **selector ยืดหยุ่นกว่า**  

**ความแตกต่างสำคัญ:**  
| Feature | RC | RS |
|---------|----|----|
| Selector | Equality-based | Set-based & Equality-based |

**ตัวอย่าง YAML (RS) ปัจจุบัน (apps/v1):**  
```yaml
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: backend-rs
spec:
  replicas: 3
  selector:
    matchLabels:
      tier: Backend
    matchExpressions:
      - key: tier
        operator: In
        values:
          - Backend
  template:
    metadata:
      labels:
        tier: Backend
    spec:
      containers:
      - name: app
        image: my-backend:latest
```

**อธิบาย matchExpressions:**  
- `key: tier, operator: In, values: [Backend]`  
- เลือก Pod ที่มี label `tier` และค่าอยู่ใน `[Backend]`  
- สามารถใส่หลายค่า เช่น `[Backend, Frontend]` เพื่อควบคุมหลายกลุ่มพร้อมกัน

---

## 3️⃣ Best Practice ปัจจุบัน

- **ReplicationController**: เก่าแล้ว ไม่แนะนำให้ใช้  
- **ReplicaSet**: ใช้ผ่าน **Deployment** แทน ไม่ค่อยสร้างตรงๆ ด้วยมือ  
- ลำดับการทำงานจริง:  
```
Deployment --> ReplicaSet --> Pod
```
- Deployment สั่ง ReplicaSet สร้าง/ควบคุมจำนวน Pod  
- RS ทำหน้าที่รักษาจำนวน Pod ตามที่ Deployment กำหนด  

> 💡 สรุป: RS คือผู้จัดการจำนวน Pod, Deployment คือ Controller ระดับสูงที่ควบคุม RS เพื่อให้เราใช้งานง่ายและปลอดภัยยิ่งขึ้น

