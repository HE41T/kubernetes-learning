# Kubernetes Node คืออะไร (อัปเดตข้อมูลล่าสุด)

บทความนี้อธิบายเกี่ยวกับ **Node** ซึ่งเป็นหน่วยทำงานหลักใน Kubernetes Cluster โดยปรับปรุงคำอธิบายให้ถูกต้องตามมาตรฐานปัจจุบัน (K8s 1.30+ รวมถึง 1.34)

---

## 1. Node คืออะไร?
**Node** คือเครื่องคอมพิวเตอร์ที่ใช้รันงานจริงใน Kubernetes โดยอาจเป็น:
- Server จริง (Physical Machine)
- Virtual Machine (VM)
- Cloud Instance (เช่น AWS EC2, GCP Compute Engine, Azure VM)

**หน้าที่ของ Node:**
- เป็นสถานที่ที่ Kubernetes Scheduler จะนำ **Pod** มาวางเพื่อให้รันงานจริง

> หมายเหตุ: คำว่า *Minion* เป็นชื่อเรียกเก่าสมัย Kubernetes รุ่นแรก ปัจจุบันไม่ใช้แล้ว

---

## 2. สิ่งที่ต้องมีใน Node
เพื่อให้ Node ทำงานได้ จะต้องติดตั้งองค์ประกอบหลัก 3 อย่างนี้:

### 🔹 1. Kubelet
เป็น "หัวหน้าคนงาน" บน Node
- ติดต่อสื่อสารกับ Control Plane (Master)
- รับคำสั่ง, สร้าง/ลบ Pod
- ตรวจสุขภาพ Pod และรายงานสถานะให้ Control Plane รู้

### 🔹 2. Kube-proxy
เป็น "คนดูแลระบบเครือข่ายของ Pod"
- จัดการ rules ต่าง ๆ (เช่น iptables หรือ IPVS)
- ทำให้ Service สามารถ forward traffic ไปยัง Pod ได้

### 🔹 3. Container Runtime
เป็นตัวรัน container เช่น:
- **containerd (มาตรฐานปัจจุบัน)**
- CRI-O
- Docker Engine (ผ่าน dockershim – ปัจจุบันเลิกใช้แล้ว)

---

## 3. Node ถูกสร้างขึ้นอย่างไร? (Node Creation)
Kubernetes **ไม่สร้างเครื่อง Server เอง** แต่รอให้คุณเตรียมเครื่องก่อน เช่น:
- สร้าง VM บน Proxmox/VMWare
- สร้างเครื่องบน Cloud (EC2, GCP, Azure)
- ติดตั้ง Ubuntu บน Server จริง

เมื่อเครื่องพร้อมแล้ว จึงใช้คำสั่ง:
```
kubeadm join
```
เพื่อให้เครื่องนั้นกลายเป็น Node ใน Cluster

Kubernetes เองมีหน้าที่ "บริหารจัดการ Node" ไม่ใช่สร้าง Node

---

## 4. Node Controller คืออะไร?
**Node Controller** เป็นส่วนหนึ่งของ `kube-controller-manager` (รันบน Control Plane) ทำหน้าที่:

### ✔ ตรวจสุขภาพ Node (Node Health Check)
- เช็คสัญญาณ Heartbeat จาก kubelet
- ถ้าหายไปนาน (ค่า default ~40 วินาที): mark เป็น `NotReady`
- ถ้าหายไปนานมาก ๆ: อาจเริ่มลบ Pod ออกจาก Node

### ✔ ตัดสินใจส่งงานให้หรือไม่
- Node ปกติ → Scheduler สามารถส่ง Pod ไปให้รันได้
- Node ผิดปกติ / NotReady → Scheduler จะเลี่ยงไม่ใช้งาน

### ✔ จัดการการลบ Node
ถ้า Node ตายไปนานมาก ๆ จะโดน mark ว่า unreachable

---

## 5. รูปแบบข้อมูล Node (YAML/JSON)
Node เป็น Object ประเภทหนึ่งใน Kubernetes API เช่นเดียวกับ Pod, Service, Deployment

ตัวอย่างโครงสร้าง:
```yaml
apiVersion: v1
kind: Node
metadata:
  name: worker-node-1
  labels:
    node-role.kubernetes.io/worker: ""
spec:
  ...
status:
  ...
```
ชื่อของ Node มักจะเป็น **Hostname** ของเครื่อง ไม่ใช่ IP (ปัจจุบันใช้ Hostname เป็นหลัก)

---

## 6. การลงทะเบียน Node (Registration)
Node สามารถ join เข้า cluster ได้ 2 แบบ:

### ✔ 1) Auto Registration (ค่า default)
เมื่อสั่ง:
```
kubeadm join
```
- Control Plane จะ "รับ Node เข้า cluster อัตโนมัติ"
- kubelet จะดึง certificate และข้อมูล config จาก Master

เป็นวิธีมาตรฐานและนิยมที่สุด

### ✔ 2) Manual Registration
ใช้เมื่อ admin ต้องการควบคุมมากเป็นพิเศษ เช่น:
- เขียน Node manifest ไว้เองในไฟล์ YAML
- แล้วใช้ `kubectl apply -f node.yaml`

ส่วนใหญ่ไม่ค่อยใช้ ยกเว้นในระบบที่ต้องการ security ระดับสูง

---

## 📌 สรุปสั้น ๆ
- **Node** = เครื่อง Server คนงานที่ใช้รัน Pod
- ต้องมี: **kubelet**, **kube-proxy**, **containerd/CRI-O**
- Kubernetes ไม่ได้สร้าง Node เอง แต่จัดการ Node ที่สร้างไว้แล้ว
- **Node Controller** คอยเช็คสุขภาพและกำหนดว่า Node ใช้งานได้หรือไม่
- Node สมัครเข้า cluster ผ่าน `kubeadm join` โดยปกติจะ auto-register

ต้องการให้เพิ่มหัวข้อเช่น **Node Condition**, **Taint/Toleration**, **Cordon/Drain**, หรือ **Node Maintenance Guide** ไหมครับ?